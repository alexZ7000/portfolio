# Deploy is now handled inside CI.yml as a dependent job.
# This file is kept as a stub (manual dispatch only) to avoid orphan references.
name: Deploy (deprecated)

on:
    workflow_dispatch:

jobs:
    noop:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Deploy is handled by CI.yml. Delete this file at your convenience."
